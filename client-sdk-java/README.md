# client-sdk-java

Java prompt loading library for promptLM.

This module keeps the existing Java loader behavior and remains buildable through the workspace root Maven parent.

Release instructions are documented in [`../docs/releasing-java.md`](../docs/releasing-java.md).
