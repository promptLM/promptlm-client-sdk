<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.promptlm</groupId>
    <artifactId>promptlm-client-sdk-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>promptlm-client-sdk-parent</name>
    <description>Standalone promptLM client SDK project</description>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <argLine></argLine>
        <surefire.extraArgs></surefire.extraArgs>
        <maven-surefire-plugin.version>3.5.1</maven-surefire-plugin.version>
        <spring-boot.version>3.5.4</spring-boot.version>
    </properties>

    <modules>
        <module>client-sdk-java</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <argLine>${argLine} ${surefire.extraArgs}</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>enable-dynamic-agent-loading</id>
            <activation>
                <jdk>[21,)</jdk>
            </activation>
            <properties>
                <surefire.extraArgs>-XX:+EnableDynamicAgentLoading</surefire.extraArgs>
            </properties>
        </profile>
    </profiles>
</project>
